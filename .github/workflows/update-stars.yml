# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/update-stars.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  refresh-stars:
    permissions:
      contents: write
      id-token: write
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@main
      with:
        fetch-depth: 1
        persist-credentials: true
    - name: Install Nix
      uses: nixbuild/nix-quick-install-action@master
    - name: Magic Nix Cache(Use GitHub Actions Cache)
      uses: DeterminateSystems/magic-nix-cache-action@main
    - env:
        CODEBERG_TOKEN: ${{ secrets.CODEBERG_TOKEN }}
        GITHUB_TOKEN: ${{ secrets.STARS_GH_TOKEN }}
        GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}
      name: Generate stars.md
      run: nix run .#update-stars
    - name: Commit refreshed README.md
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: 'docs(stars): refresh GitHub, Codeberg & GitLab stars in README'
        file_pattern: README.md
    timeout-minutes: 30
'on':
  schedule:
  - cron: 0 0 * * 0,3
  workflow_dispatch: {}
